{
  "name": "@ui-annotator/mcp-server",
  "version": "1.0.0",
  "description": "MCP server that receives annotated UI feedback from the Chrome extension",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
