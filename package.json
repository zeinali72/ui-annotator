{
  "name": "ui-annotator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/mcp-server",
    "packages/chrome-extension"
  ],
  "scripts": {
    "test": "npm test --workspace=packages/mcp-server"
  },
  "engines": {
    "node": ">=18"
  }
}
